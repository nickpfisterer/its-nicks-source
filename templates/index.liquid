{% layout "default" %}
{% block content %}
	{% collection "blog" by 5 query 'tags={{ mixture.route.tag }}' order desc %}
		{% if collection.size == 0 %}
			{% include "no-articles" %}
		{% else %}
			{% assign currentpage = collection.current_page %}
			{% for article in collection.items %}
				{% include "article" %}
			{% endfor %}
			{% if collection.size > 5 %}
				{% include "pagination" %}
			{% endif %}
		{% endif %}
	{% endcollection %}
{% endblock %}
